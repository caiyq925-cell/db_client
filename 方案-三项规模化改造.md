# 方案：结构树规模化 · Mongo 3.2 只读层 · 大表浏览与流式导出

> 日期：2026-09-11 · 状态：待评审
> 前置结论（已对齐）：MongoDB 3.2 必须支持但仅限**只读浏览查询**（账号密码认证）；Redis 集群支持顺延至下一周期。

***

## 一、问题陈述

| # | 缺口            | 现状                                                        | 与规模的冲突                          |
| - | ------------- | --------------------------------------------------------- | ------------------------------- |
| A | 结构树不可用        | [SchemaTab.tsx](../src/SchemaTab.tsx) 三栏全量 DOM 渲染，无过滤、无搜索 | 几百库 × 3000 表 × 300 列时渲染卡死、找表靠肉眼 |
| B | Mongo 3.2 连不上 | 官方 `mongodb = "3"` 驱动支持底线 3.6（无 OP\_MSG）                  | 3.2 WiredTiger 实例完全无法使用         |
| C | 大表浏览与导出       | 数据表固定 `LIMIT 200` 无翻页；导出为前端拼接整块字符串后 Blob 下载               | 上亿行表翻不了页、导出直接撑爆前端内存             |

## 二、解决方案总览

```
┌─ 前端 React ─────────────────────────────────────────────┐
│  A: 三栏虚拟化 + 过滤框 + 跨库搜表                          │
│  C: 数据表 keyset 翻页 UI + 流式导出弹窗（进度/取消）        │
├─ Tauri commands ─────────────────────────────────────────┤
│  A: +search_tables(pattern)                               │
│  C: +export_stream(...)，事件 export-progress              │
├─ drivers ────────────────────────────────────────────────┤
│  B: mongo_legacy.rs（纯 Rust 旧协议 OP_QUERY/OP_REPLY）    │
│     MongoDriver 内部 enum Backend { Official | Legacy }   │
│  C: mysql.rs +keyset 分页查询、流式批量 fetch               │
└──────────────────────────────────────────────────────────┘
```

原则：后端结构查询已按需（`SHOW DATABASES` / `information_schema` 单库查询），A 的瓶颈纯在前端；B 零 C 依赖；C 在 Rust 侧流式化。

***

## 三、Feature A：结构树规模化

### 实现决策

* **保留三栏布局**（库 | 表 | 详情），不做树形控件。理由：库切换本身就是一次按需查询，三栏在「几百库」场景优于单树；零交互习惯迁移成本。

* **A1 虚拟化**：库列表、表列表改为自实现 windowing（固定行高 28px，仅渲染可见区 ±10 行）。不引入 react-window 等依赖，核心是一个纯函数 `visibleRange(scrollTop, viewportH, rowH, total) -> (start, end)`，可单测。

* **A2 表过滤框**：表列表顶部输入框，客户端 substring 过滤（3000 项内存过滤瞬时，防抖 150ms）。

* **A3 跨库搜表**：工具栏搜索图标 → 弹层输入 → 调新命令 `search_tables`。结果列出 `库.表（约 N 行）`，点击直接打开表详情/数据页。

* 表详情的列区（≤300 行）保持普通表格 + max-height 滚动，不虚拟化（收益低）。

### 接口契约

```rust
#[tauri::command]
async fn search_tables(conn: DatabaseConnection, pattern: String)
    -> Result<Vec<TableRef>, String>;
// TableRef { schema: String, name: String, row_count: Option<u64> }
```

MySQL 实现（一次查询，无需认证额外开销）：

```sql
SELECT TABLE_SCHEMA, TABLE_NAME, TABLE_ROWS
FROM INFORMATION_SCHEMA.TABLES
WHERE TABLE_TYPE='BASE TABLE' AND TABLE_NAME LIKE ? AND TABLE_SCHEMA NOT IN
  ('mysql','information_schema','performance_schema','sys')
ORDER BY TABLE_ROWS DESC LIMIT 100
```

Mongo 实现：`list_database_names` 全量 + 逐库 `list_collection_names`？——否，改为仅列出库名供用户切换（Mongo 无跨库集合元数据查询；文档写明差异）。

### 用户故事

* 作为用户，我想要在 3000 张表的库中输入 `order` 立刻筛出相关表，以便不用肉眼扫列表。

* 作为用户，我想要跨几百个库搜索表名，以便不记得表在哪个库时也能定位。

* 作为用户，我想要列表只渲染可见行，以便大实例上滚动不卡顿。

***

## 四、Feature B：MongoDB 3.2 只读层（纯 Rust 旧协议）

### 设计要点

3.2（maxWireVersion=4）已支持命令式只读面：`find`（3.2 新增）、`count`、`listCollections`、`listIndexes`、`listDatabases`。仅需实现三个旧版 wire op：

| Op                                | 码           | 用途                                |
| --------------------------------- | ----------- | --------------------------------- |
| OP\_QUERY                         | 2004        | 握手 isMaster + 所有 `$cmd` 命令        |
| OP\_REPLY                         | 1           | 响应解析（cursorId + BSON 文档流）         |
| OP\_GET\_MORE / OP\_KILL\_CURSORS | 2005 / 2007 | 游标续读与关闭（find 命令 batchSize 给大后仅兜底） |

* **传输**：tokio `TcpStream` + 4 字节消息头（length/requestId/responseTo/opCode），BSON 编解码用官方 `bson` crate（独立可用）。3.2 不启用压缩，遇 `OP_COMPRESSED` 报「请关闭连接压缩」。

* **认证**：先 SCRAM-SHA-1（`saslStart/saslContinue`，RFC 5802，hmac/sha1/rand，约 200 行），失败且服务端拒绝机制时回退 MONGODB-CR（`md5(user + ":mongo:" + pass)`，约 10 行）。对用户透明。

* **路由判定（自动，无需用户选择）**：连接时先做一次无认证 `isMaster` 探测（1 RTT）：

  * `maxWireVersion >= 6` → 官方 mongodb 驱动路径（现有全部能力）

  * `4 ≤ maxWireVersion < 6` → legacy 只读路径

  * `< 4` → 报错「仅支持 MongoDB 3.2+」

* **封装**：[mongo.rs](../src-tauri/src/drivers/mongo.rs) 内部改持 `enum Backend { Official, Legacy }`，上层 command 面（`list_databases` / `list_collections` / `get_collection_schema` / `execute_query`）签名不变，前端无感知。

* **只读强制**：legacy 后端不实现任何写命令；QueryTab 输入的 JSON 直接作为 `find.filter`。用户输入聚合管道/命令时返回明确错误：「MongoDB 3.2 只读层仅支持 filter 查询」。

* 连接复用：legacy client 单 TcpStream 串行命令，沿用现有「每 command 建连」模式（池化重构为独立优化项，本周期不做）。

### 连接状态机

```
TCP连接 ──失败──► ERROR(网络)
  │ 成功
isMaster探测 ──wireVersion<4──► ERROR(不支持)
  │ ≥4
认证(SCRAM→CR回退) ──失败──► ERROR(认证)
  │ 成功
READY ──协议帧校验失败/断连──► ERROR(协议)，连接置死需重连
```

### 异常处理矩阵

| 异常                   | 检测                       | 处理            | 用户呈现                                 |
| -------------------- | ------------------------ | ------------- | ------------------------------------ |
| 服务端返回 OP\_COMPRESSED | opCode=2012              | 断连            | 「请在服务端关闭 compression」                |
| SCRAM 且 CR 均失败       | 服务端错误码                   | 终止            | 「认证失败（已尝试 SCRAM-SHA-1 / MONGODB-CR）」 |
| find 游标 getMore 失败   | responseTo/长度错或网络断       | 返回已取批次 + 截断标记 | 结果表标注「游标中断，已显示 N 行」                  |
| 读超时                  | tokio timeout（默认 30s，可配） | 断连            | 「连接超时」                               |
| 用户在 3.2 上发聚合/命令      | legacy 后端入口拦截            | 拒绝执行          | 「3.2 只读层仅支持 filter 查询」               |

### 用户故事

* 作为用户，我想要用账号密码连接 3.2 实例并浏览库/集合/文档，以便老实例仍在同一个工具里管理。

* 作为用户，我想要工具自动识别 3.2 并自动降级为只读，以便不需要手动选择协议版本。

***

## 五、Feature C：大表浏览与流式导出

### C1 行数策略

* 默认展示 `information_schema.TABLES.TABLE_ROWS` **估算值**，标注「约 N 行」。

* 「精确计数」按钮 → `SELECT COUNT(*)`（亿级 InnoDB 全扫可能数十秒，按钮旁提示，结果缓存于 tab 状态）。

### C2 keyset 翻页（替代固定 LIMIT 200）

```
有主键（含复合主键）：
  首页:  SELECT * FROM s.t ORDER BY pk ASC LIMIT 201
  下一页: WHERE (pk…) > (:last_pk) ORDER BY pk ASC LIMIT 201      -- 行值比较，5.7/8.0 均支持
  上一页: WHERE (pk…) < (:first_pk) ORDER BY pk DESC LIMIT 201 → 反转
  多取 1 行判定 hasMore，第 201 行丢弃
无主键：
  LIMIT 201 OFFSET n，工具栏提示「无主键，深翻页可能缓慢」
```

* 页栈：每页记录首/尾 PK 值（`Vec<(first,last)>`），支撑双向翻页，O(1) 回退。

* 编辑兼容：`update_rows` 按 PK 定位行，翻页不影响；脏行状态随页保留在页栈快照内（离开页丢弃未保存修改，弹确认）。

* 本周期排序固定 PK 升序；任意列排序/条件筛选列**范围外**。

### C3 流式导出（Rust 侧）

```rust
#[tauri::command]
async fn export_stream(
    conn: DatabaseConnection,
    source: ExportSource,        // Table{schema,table} | Query{sql}
    format: ExportFormat,        // Csv | JsonLines
    path: String,                // 前端经 tauri-plugin-dialog save 得到
    max_rows: u64,               // 默认 5000 万，防误操作，可在弹窗调大
) -> Result<ExportSummary, String>;
```

* 循环批次（默认 5000 行）：`BufWriter` 追加写盘；CSV 用 `csv` crate 手写转义规则，JSONL 逐行 `serde_json`。内存占用恒定 O(batch)。

* 进度：每批 emit `export-progress { rows, max_rows }`；取消：前端置 `AtomicBool`，批间检查。

* 表级导出走 keyset 循环（复用 C2），无 PK 表退化为 OFFSET 循环（带警告）。

* **不做 xlsx**：Excel 硬上限 1,048,576 行，对亿级表无意义；CSV/JSONL 通用。查询结果网格的小结果集导出保留现有前端路径。

* 导出弹窗：格式选择、行上限输入、开始后进度条 + 取消按钮 + 完成摘要（行数/耗时/路径）。

### 导出状态机

```
IDLE ──开始──► RUNNING ─每批──► emit进度 ─┐
  ▲                │ 取消                │ 完成(max_rows或游标尽)
  └── reset ◄── ABORTED ◄──────────────── DONE / ERROR(写盘失败)
```

### 用户故事

* 作为用户，我想要在上亿行的表里按主键流畅翻页，以便不被深分页卡死。

* 作为用户，我想要把一张大表流式导出为 CSV 并看到进度、可中途取消，以便导出过程可控不爆内存。

***

## 六、测试决策（TDD，垂直切片：一个测试 → 一个实现）

| 特性 | 测试面                            | 关键用例                                                                       |
| -- | ------------------------------ | -------------------------------------------------------------------------- |
| A  | 前端纯函数单测（vitest）                | `visibleRange` 边界（顶部/底部/越界）；过滤防抖后结果                                        |
| A  | MySQL 集成测试（真实实例）               | `search_tables` 命中/无结果/系统库过滤/LIMIT 100                                     |
| B  | Rust 集成测试（真实 3.2 实例，需用户提供测试账号） | 探测 wireVersion=4；SCRAM 登录成功；CR 回退；find 分批；listCollections/listIndexes；聚合被拒 |
| C  | MySQL 集成测试                     | keyset 三页往返一致性；复合主键；无主键退化；`多取1行` hasMore 边界                                |
| C  | 导出单测+集成                        | CSV 转义（逗号/引号/换行/NULL）；batch 不整除；取消后文件截断正确；max\_rows 中断返回摘要                 |
| C  | 前端                             | 页栈回退状态正确；脏行离开页面弹确认                                                         |

实现时需向用户索取：3.2 实例测试连接串（账号+密码）、一个大表所在的 MySQL 测试连接。

## 七、里程碑（垂直切片，每片可独立验证）

1. **切片1 · A 全部**：`search_tables` command → 过滤框 → 虚拟化。纯前端 + 1 个 command，风险最低收益立现。✅ 已完成（2026-09-12）
2. **切片2 · C1+C2**：估算行数 + keyset 翻页（mysql.rs + store.ts + DataTableTab UI）。✅ 已完成（2026-09-12）
3. **切片3 · C3**：流式导出 command + 进度事件 + 导出弹窗（含 dialog 插件接入）。✅ 已完成（2026-09-12，表导出改为单条流式 SELECT，见实现偏离说明）
4. **切片4 · B 全部**：mongo\_legacy.rs 协议 → 认证 → Backend 路由 → 前端接线。✅ 已完成（2026-09-12，纯实现+单测；3.2 实例集成验证待用户提供测试账号）→ ✅ 集成验证完成（2026-09-12，真实 3.2.10 实例 6/6 用例全过）

### 切片4 · B 实现要点（2026-09-12 落地）

* `drivers/mongo_legacy.rs`（约 730 行）：OP_QUERY/OP_REPLY/OP_KILL_CURSORS 纯 Rust 实现；SCRAM-SHA-1（RFC 5802/6070/2202 测试锚定，服务端签名经 Python hashlib 独立验证）+ MONGODB-CR 自动回退；只读 listDatabases / listCollections / find。
* `drivers/mongo_backend.rs` 路由层：连接前用一次性 legacy 连接探测 `maxWireVersion`（isMaster 走 admin.$cmd，全版本可用）→ wire ≥ 6 走官方 mongodb 驱动，wire < 6（3.2）走 legacy 层；ConnectionString 场景直连官方驱动；SSH 隧道 host/port 处理与 mysql/redis 一致。
* 7 个 Mongo command 分支（test_connection / execute_query / get_schemas / list_tables / get_table_columns / get_table_detail / search_tables）全部改走 `MongoBackend`；command 契约不变，前端零改动。
* legacy 侧集合字段推断复用官方驱动的 `infer_fields`（bson 2.15 与 mongodb 3.9 共用同一 bson crate，Document 类型互通）。
* 测试：lib 单测 36（含 mongo_legacy 14）+ mysql 集成 15 全绿；SCRAM 链曾有期望值错误（`YkSivs...` 来自误传），以 Python 独立计算值 `NI6Qel39...` 为锚定后修正。

### 切片4 · B 集成验证结论（2026-09-12，真实 3.2.10 实例）

* `tests/mongo_integration.rs` 6 用例全过：wire 探测=4、buildInfo、认证+listDatabases（44 业务库）、listCollections+find 采样（order 库）、JSON 查询、错误密码拒绝。
* **SCRAM 修复（真实抓包定位）**：3.2 在 client-final 回复中即返回 `v=`（done=false），随后要求一次空 saslContinue 收尾（回复为空）。原实现误取第二次空交换的 payload。已用 pymongo 3.13 手动逐字节复刻对照确认。
* **authSource 解耦**：认证库恒为 admin，`conn.database` 仅为默认工作库（UI 每次查询会覆盖），此前把工作库当认证库导致切库后认证失败。
* MONGODB-CR 回退实现按 mgo/Java 驱动公式重写（`key = md5(nonce + user + ":mongo:" + md5hex(user:mongo:pass))`，payload 为 Binary 包裹的 `{user, nonce, key}` 文档）；本实例服务器禁用 CR（"mechanism not supported"），回退路径保留给真正老格式的用户。
* 腾讯云实例限制（非代码问题）：账号可列 admin 集合但读 admin 数据报 error 73 "not allowed to visit admin"；业务库（order/df_member 等）完全可读。

## 八、范围外（本周期不做）

* Redis 集群支持与键空间浏览（下一周期，已对齐顺延）

* 连接池/长连接复用重构（所有 driver 现为每 command 建连）

* 数据表任意列排序、条件筛选、跨表 JOIN 视图

* Excel (xlsx) 导出（Excel 行数硬上限 104 万，对亿级表无意义）

* Mongo 3.2 写操作、聚合管道调试、Mongo 跨库集合搜索

* 敏感数据脱敏、权限分级（个人自用，无需）

## 九、附带订正

* [README.md](../README.md) 技术栈表写 React 18，实际 package.json 为 React 19，随切片 1 顺带订正。

