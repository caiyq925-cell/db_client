# DBClient 数据库管理工具 · 视觉美学诊疗报告

> **应用定位**：开发者 / DBA 高频驻留型生产力工具（深色 IDE 范式）
> **技术推断**：Electron / Tauri 类 Web 内核桌面应用
> **使用场景**：长时间凝视代码与数据，多库并行查询，键盘驱动操作
> **报告版本**：v1.0 ｜ 出具人：数字界面美学总监

---

## 一、五维度视觉审计（问题清单）

### 1.1 色彩诊断

| 编号 | 病灶描述 | 严重度 | 影响范围 |
|------|----------|--------|----------|
| C-01 | 背景层级塌陷——侧边栏、编辑区、结果区近乎同色（约 `#1E1E24`），仅靠 1px 细线勉强分界，空间疆域模糊 | 🔴 P0 | 全局认知负荷 |
| C-02 | 主色蓝（执行按钮 `#2D7FF9` 附近）饱和度过高，在暗底上形成刺眼光斑，长时间注视引发虹膜疲劳 | 🟠 P1 | 视觉锚点失衡 |
| C-03 | 连接状态绿点（`#2ECC71` 级）与主色蓝无色系呼应，语义色孤立漂浮 | 🟡 P2 | 品牌一致性 |
| C-04 | SQL 语法高亮配色（紫关键字 / 橙数字）取自默认主题，未与本应用色板校准，紫橙蓝三色争艳 | 🟠 P1 | 代码可读性 |
| C-05 | 占位提示文字「输入 SQL 后按 Ctrl+Enter 执行」灰度过低（约 `#555555`），对比度逼近 WCAG AA 下限 | 🟡 P2 | 可访问性 |

### 1.2 排版诊断

| 编号 | 病灶描述 | 严重度 | 影响范围 |
|------|----------|--------|----------|
| T-01 | 数据库列表项行高过密，连续 30+ 同质条目形成「文字墙」，扫视时易串行 | 🔴 P0 | 核心导航效率 |
| T-02 | 字体谱系混杂——标题用系统黑体、代码用等宽、中文混排字重不统一，缺乏谱系约束 | 🟠 P1 | 专业感 |
| T-03 | 顶部 Tab「查询 - MySQL 测试环境」与工具栏「MySQL 测试环境 (mysql)」信息冗余重复 | 🟡 P2 | 信息密度 |
| T-04 | 「限 500 行」下拉与执行按钮字号、字重同级，主次未分 | 🟡 P2 | 操作层级 |

### 1.3 空间诊断

| 编号 | 病灶描述 | 严重度 | 影响范围 |
|------|----------|--------|----------|
| S-01 | 侧边栏列表项左侧缩进与图标间距非 8 倍数，视觉节奏参差 | 🟠 P1 | 秩序感 |
| S-02 | 编辑区与下方结果区以硬分割线切分，但结果区空置时大片死黑，空间利用率与呼吸感双失 | 🔴 P0 | 首屏观感 |
| S-03 | 顶部工具栏高度与 Tab 栏高度比例失调，工具栏偏矮显局促 | 🟡 P2 | 控件舒适度 |
| S-04 | 滚动条为系统原生粗样式，与精致暗色界面气质冲突 | 🟡 P2 | 细节打磨 |

### 1.4 组件诊断

| 编号 | 病灶描述 | 严重度 | 影响范围 |
|------|----------|--------|----------|
| K-01 | 数据库图标全部为同一灰色圆柱，30 个库无任何视觉区分，认知全靠读文字 | 🔴 P0 | 检索效率 |
| K-02 | 「执行」按钮为唯一强填充色块，其余控件皆幽灵态，组件权重两极分化 | 🟠 P1 | 层级表达 |
| K-03 | 选中态（MySQL 测试环境高亮条）圆角与悬停态缺失过渡，状态切换生硬 | 🟡 P2 | 交互质感 |
| K-04 | 筛选输入框内嵌于列表，边框与背景对比不足，可发现性弱 | 🟡 P2 | 功能可达性 |

### 1.5 动效诊断

| 编号 | 病灶描述 | 严重度 | 影响范围 |
|------|----------|--------|----------|
| M-01 | 树节点展开 / 折叠为瞬切，无高度过渡，列表跳动突兀 | 🟠 P1 | 操作流畅度 |
| M-02 | 查询执行无进度 / 状态反馈动效，用户失去「确定性」感知 | 🔴 P0 | 心理安全感 |
| M-03 | Tab 切换、面板分割拖拽缺乏缓动，工具感粗糙 | 🟡 P2 | 品质感 |

### 1.6 严重度分级说明

- 🔴 **P0 致命**：直接阻碍任务完成或首屏观感崩坏
- 🟠 **P1 重症**：显著削弱效率与专业感
- 🟡 **P2 慢疾**：长期驻留引发疲劳或细节欠打磨

---

## 二、美学基调确立

**关键词**：**深邃 · 秩序 · 让位 · 确定**

> 数据库工具之本质，是让人沉浸于数据与逻辑。界面当如暗夜之砚台——墨色分层而不喧宾，朱砂一点（执行）而气韵自生。代码与数据为主角，界面框架（chrome）须退居二线。

- **色彩情绪板**：冷墨三层底 + 单青主色 + 语义四色（连接 / 断开 / 错误 / 警告）
- **空间气质模型**：标准密度 **8px 基准**；列表行高放宽至 28px 破文字墙；编辑区舒展留白

---

## 三、原子级设计规范（设计令牌）

### 3.1 色彩系统（深色三层墨阶）

```css
:root {
  /* ── 背景三层：制造空间纵深 ── */
  --bg-base:     #16161A;  /* 最底层：侧边栏、状态栏、结果区 */
  --bg-surface:  #1C1C21;  /* 中层：编辑区、面板 */
  --bg-elevated: #24242B;  /* 表层：卡片、下拉、悬停 */
  --bg-overlay:  #2C2C35;  /* 浮层：弹窗、Tooltip */

  /* ── 描边三级 ── */
  --border-subtle:  #2A2A32;
  --border-default: #353540;
  --border-strong:  #45454F;

  /* ── 文字四级 ── */
  --text-primary:   #E8E8ED;  /* 正文，对比度 ≥ 4.5:1 */
  --text-secondary: #A0A0AB;  /* 次要信息 */
  --text-tertiary:  #6B6B76;  /* 辅助 / 占位 */
  --text-disabled:  #4A4A54;  /* 禁用 */

  /* ── 主色：降饱和青蓝，护眼（由原 #2D7FF9 调整） ── */
  --primary:       #4C8DFF;
  --primary-hover: #6BA0FF;
  --primary-press: #3A73E0;
  --primary-muted: rgba(76, 141, 255, 0.12);

  /* ── 语义色：克制 ── */
  --success: #34D399;  /* 连接正常 */
  --danger:  #F87171;  /* 错误 / 断开 */
  --warning: #FBBF24;  /* 警告 */
  --info:    #60A5FA;  /* 信息 */

  /* ── SQL 语法高亮：统一色温（由默认主题校准） ── */
  --syn-keyword:  #B48EAD;  /* 关键字：柔紫（原 #C792EA 降饱和） */
  --syn-string:   #A3BE8C;  /* 字符串：苔绿 */
  --syn-number:   #E0A070;  /* 数字：暖赭（原 #D08770 提亮） */
  --syn-function: #88C0D0;  /* 函数：青 */
  --syn-comment:  #5C6370;  /* 注释 */
}

### 3.2 间距与尺度（8px 基准）

:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;

  /* 关键尺寸 */
  --row-height-list: 28px;  /* 数据库列表行高（原 24px → 破文字墙） */
  --toolbar-height:  44px;  /* 工具栏（原偏矮 → 放宽） */
  --tab-height:      36px;
  --sidebar-width:   264px;

  /* 圆角三阶 */
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;
}

### 3.3 字体谱系（收敛为 4 级字阶）

:root {
  --font-ui:   "Inter", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif;
  --font-mono: "JetBrains Mono", "SF Mono", "Consolas", monospace;

  --t-h1:   600 15px/1.4 var(--font-ui);    /* 面板标题 */
  --t-body: 400 13px/1.5 var(--font-ui);    /* 列表 / 正文 */
  --t-code: 400 13px/1.6 var(--font-mono);  /* SQL 编辑区 */
  --t-meta: 400 12px/1.4 var(--font-ui);    /* 辅助 / 计数徽标 */
}

### 3.4 阴影与动效 
:root {
  --shadow-float: 0 8px 24px rgba(0, 0, 0, 0.4),
                  0 2px 6px  rgba(0, 0, 0, 0.3);

  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-std: cubic-bezier(0.4, 0, 0.2, 1);

  --dur-fast: 120ms;
  --dur-base: 200ms;
  --dur-slow: 280ms;
}

四、逐层重构方案
4.1 骨架网格与留白（定疆域）
三区墨阶分明：侧边栏 --bg-base #16161A ｜ 编辑区 --bg-surface #1C1C21 ｜ 结果区 --bg-base；以色差而非硬线分界，硬线降级为 --border-subtle 仅作辅助。
列表行高 24px → 28px：每项上下各增 2px 呼吸，30 库不再成墙。
空结果区：移除死黑，置入居中「待命」占位——大号淡化图标（透明度 8%）+ --text-tertiary 提示文案，赋予待命语义而非空洞感。
4.2 色彩层级与对比（定气质）
执行按钮主色降至 #4C8DFF，并改为状态化强调：常态使用 --primary-muted 底 + 主色文字；仅「SQL 就绪可执行」时实填主色——让蓝色成为状态信号而非常驻光斑。
连接绿点改为 --success #34D399，外加 2px 同色 20% 透明光晕，暗示「活跃」。
占位文字由 #555555 提至 --text-tertiary #6B6B76，达到 WCAG AA。
4.3 字体谱系与字重（定声调）
数据库名统一 --font-mono 13px：库名本即标识符，等宽更专业且纵向对齐整齐。
计数徽标「1」使用 --t-meta + --bg-elevated 胶囊底：弱化但可读。
顶部 Tab 与工具栏去重：Tab 仅留「查询」+ 库名缩写；工具栏显示连接全称。
4.4 组件重塑
库图标差异化：按库名前缀语义着色——ai_* 系用青、activity* 用绿、archive* 用赭；取 8% 饱和度色块底 + 同色描边图标，30 库瞬间可分组扫视。
筛选框：边框升至 --border-default；聚焦态加 --primary 1px 环 + --primary-muted 底。
滚动条：自定义 6px 宽、--bg-overlay 滑块、悬停展宽至 8px、圆角 3px。
4.5 动效语义与状态映射（定呼吸）

| 交互 | 动效规范 |
|------|----------|
| 树节点展开 / 折叠 | 高度 + 透明度联动，`200ms var(--ease-out)`，子项 stagger 30ms 渐入 |
| 列表项悬停 | 背景 `transparent → --bg-elevated`，`120ms var(--ease-std)` |
| 选中态 | 左侧 2px `--primary` 指示条 + `--primary-muted` 底，滑入 `200ms` |
| 查询执行 | 按钮内置 1.5px 进度环旋转 + 文案变「执行中…」；结果区顶部 2px `--primary` indeterminate 进度条；完成时结果区 `opacity 0→1` + `translateY 4px→0`，`280ms var(--ease-out)` |
| 查询报错 | 结果区顶边闪 `--danger` 1px，`150ms` 闪烁两次；错误信息以红色左竖线标注 |
| Tab 切换 | 内容区交叉淡入淡出 `150ms`，避免硬切 |

五、前后对比示意
| 维度 | 现状 | 重构后 |
|------|------|--------|
| 空间纵深 | 三区同色，扁平一片 | 三层墨阶：侧栏沉、编辑浮、结果退 |
| 列表可读 | 24px 密排文字墙 | 28px 行高 + 前缀语义色分组，扫视提速 |
| 主色运用 | 蓝色常驻刺眼 | 蓝色转为状态信号，常态退隐 |
| 空状态 | 死黑 + 低对比灰字 | 待命语义占位，对比度达标 |
| 执行反馈 | 无任何反馈 | 进度环 + 顶部进度条 + 结果渐入，确定性拉满 |
| 语法高亮 | 紫橙蓝争艳 | 统一柔色温，久看不累 |

验收检查清单
侧边栏 / 编辑区 / 结果区肉眼可辨三层色差
数据库列表连续扫视 10 秒不串行，前缀色组可识别
执行查询时按钮进度环、顶部进度条、结果渐入三处反馈齐全
所有正文对比度 ≥ 4.5:1（用 WebAIM 校验 --text-primary on --bg-surface）
间距皆为 4 / 8 倍数，无奇数像素
键盘 Tab 可遍历筛选框、执行按钮、限流下拉，焦点环可见（2px solid var(--primary)）
夜间长时间凝视 30 分钟，主观疲劳较改版前下降
